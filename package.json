{
  "name": "basketball-viz",
  "version": "1.0.0",
  "description": "Basketball team picker and game simulator for IMA league",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "xlsx": "^0.18.5"
  }
}
